# Desktop app (portable .exe) - everything runs locally, no cloud.
fastapi==0.115.6
uvicorn==0.34.0
openpyxl==3.1.5
pywebview==5.3.2
python-multipart==0.0.20
requests==2.32.3        # road routing (OSRM) when online; straight-line fallback offline
