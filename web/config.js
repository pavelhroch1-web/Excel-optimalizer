// One-line config: after deploying the backend (see backend/README.md),
// put its real URL here. This is the only file you need to edit to point
// the GitHub Pages frontend at your backend.
window.FFO_API_BASE = "http://localhost:8000";
